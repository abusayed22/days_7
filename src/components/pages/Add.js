import React from 'react'

function Add() {
  return (
    <div>
        {/* <form className='list-group'>
            <input className='list-group-item' style={{width:350,margin:'10px 0'}} value={addState.name ||''} onChange={(e) => setAddStat({...addState,name:e.target.value})} type="text" placeholder='Enter full Name..' />
            <div style={{display:'flex',justifyContent:'space-between'}}>
              <input className='list-group-item' style={{width:120,margin:'10px 0'}} value={addState.roll ||''} onChange={(e) => setAddStat({...addState,roll:e.target.value})} type="number" placeholder='Roll:' />
            <input className='list-group-item' style={{width:200,margin:'10px 0'}} value={addState.birth ||''} onChange={(e) => setAddStat({...addState,birth:e.target.value})} type="date" placeholder='Brith Day:' />
            </div>
            <input className='list-group-item' style={{width:350,margin:'10px 0'}} value={addState.phone ||''} onChange={(e) => setAddStat({...addState,phone:e.target.value})} type="number" placeholder='Phone Number' />
           
            <textarea className='list-group-item' style={{width:350}} value={addState.address ||''} onChange={(e) => setAddStat({...addState,address:e.target.value})} id="w3review" name="w3review" rows="4" cols="50" placeholder='Address'/>

            <button onClick={() => updatehandler()} style={{width:80,margin:'10px 0'}} type="button" className="btn btn-info">Add</button>
        </form> */}
    </div>
  )
}

export default Add